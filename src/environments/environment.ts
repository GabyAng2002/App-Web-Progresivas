import { initializeApp } from "firebase/app";

// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.
  //Author: LAURA GABRIELA ANGUIANO PINEDA
export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCOtEER2mjfgN4Xp_paNUMUhO0LJnUC_SQ",
  authDomain: "practicapwa-8424a.firebaseapp.com",
  projectId: "practicapwa-8424a",
  storageBucket: "practicapwa-8424a.firebasestorage.app",
  messagingSenderId: "863005610574",
  appId: "1:863005610574:web:522cebce6f4eaf97f7a91f"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
